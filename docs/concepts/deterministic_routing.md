# Deterministic Routing

The routing demo uses explicit capability registration and deterministic matching.

It is intentionally small:
- no learned router
- explicit capability strings
- explicit HOLD / ESCALATE behavior when no bounded match exists
