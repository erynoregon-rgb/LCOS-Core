# Bounded Kernels as a Governance Surface

Bounded kernels are small execution surfaces with declared capabilities. They
make it possible to ask: what was allowed to run, why was it selected, and what
receipt was produced?

In this toy package, capabilities are simple string matches. That is deliberate.
The package shows the interface and audit form without publishing private
composition logic.

