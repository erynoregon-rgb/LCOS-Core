# Operator Kernels

Status: research-note

A bounded operator kernel is a small execution unit serving one constrained role under deterministic routing. In this public scaffold, kernels are represented only as mock implementations.
