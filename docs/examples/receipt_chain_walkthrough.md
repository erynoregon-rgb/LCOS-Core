# Receipt Chain Walkthrough

Run:

```bash
python examples/minimal_receipt_chain/demo.py
```

Expected result: two receipts written, second receipt linked to the first through `prev_hash`, chain verification returns true.
