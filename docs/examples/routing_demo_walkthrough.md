# Routing Demo Walkthrough

Run:

```bash
python examples/routing_demo/demo.py
```

Expected result:
- known capability routes to a mock kernel
- novel or unmatched content escalates or holds explicitly
